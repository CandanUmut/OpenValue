/**
 * Troy ounce, not avoirdupois. Confusing the two is a 9.7% error on every metal
 * price in the app, so the constant is defined once and imported everywhere.
 * Mirrors GRAMS_PER_TROY_OUNCE in config/universe.ts.
 */
export const GRAMS_PER_TROY_OUNCE = 31.1034768;
